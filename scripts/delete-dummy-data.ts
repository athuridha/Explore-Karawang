/**
 * DEPRECATED: Previously used to wipe all rows from 'culinary' and 'destinations' tables.
 * This script is no longer used now that data seeding/cleanup is managed differently.
 * Safe to delete.
 */
export {}
